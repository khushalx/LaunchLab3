{
  "name": "launchlab",
  "version": "1.1.0",
  "private": true,
  "description": "A browser-based startup strategy simulation with optional Groq-powered events.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check tests/engine.test.js && node --check tests/balance.test.js",
    "test": "node tests/engine.test.js && node tests/balance.test.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
